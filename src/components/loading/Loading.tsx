import React from "react";
import ReactLoading from 'react-loading';

const Loading = () => {

  return (<ReactLoading type = {'bubbles'} color = {'#F59A76'} height={100} width={100} />)
}

export default Loading