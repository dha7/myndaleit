import React from "react";
import ReactLoading from 'react-loading';
/**
 * 
 * @returns loading icon
 */
const Loading = () => {
  return (<ReactLoading type={'bubbles'} color={'rgba(242,95,47,1)'} height={100} width={100} />)
}

export default Loading